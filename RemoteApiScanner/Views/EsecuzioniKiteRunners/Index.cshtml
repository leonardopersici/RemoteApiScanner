@model IEnumerable<RemoteApiScanner.Models.EsecuzioniKiteRunner>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container text-center">
    <h1>Scanner list</h1>

    <p>
        <a class="btn btn-outline-success" asp-action="Create"><i class="bi bi-plus"></i> New scan</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th scope="col">Status</th>
                <th scope="col">Link</th>
                <th scope="col">Dictionary</th>
                <th scope="col">Errors</th>
                <th scope="col">Time</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody class="align-middle">
            @foreach (var item in Model)
            {
                <tr>
                    @if (string.IsNullOrEmpty(item.executionTime))
                    {
                        <td>
                            <div class="spinner-border text-light-emphasis fs-3" role="status" style="animation-duration: 2s"></div>
                        </td>
                    }
                    else
                    {
                        <td>
                            <i class="bi bi-check-circle-fill fs-3" style="color:green"></i>
                        </td>
                    }
                    <td>
                        @Html.DisplayFor(modelItem => item.link)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.routes)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.statusCode)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.executionTime)
                    </td>
                    <td>
                        <a class="btn btn-outline-primary" asp-action="Details" asp-route-id="@item.id"><i class="bi bi-info-circle"></i></a> |
                        <a class="btn btn-outline-danger" asp-action="Delete" asp-route-id="@item.id"><i class="bi bi-trash3"></i></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
